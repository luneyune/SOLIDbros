# Используем официальный образ Python с минимальной версией
FROM python:3.12.5

# Копируем все файлы из текущей папки в контейнер
COPY . .

# Устанавливаем зависимости из requirements.txt (если они есть)
RUN pip install --no-cache-dir -r requirements.txt

# Открываем порт (если приложение работает на нем)
EXPOSE 5000

# Команда для запуска приложения (например, Flask-приложение)
CMD ["python", "main.py"]
